services:
  linkloom:
    image: nandolawson/linkloom:latest
    restart: always
    ports:
      - 5500:80/tcp
    volumes:
      - linkloom:/srv/config:ro
    deploy:
      resources:
        limits:
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: 10m
        max-file: 3
volumes:
  linkloom:
    external: false